/**
 * DEPRECATED: Spectator API has been removed by Riot Games
 *
 * This file previously contained functions for detecting live games using the Spectator API.
 * The Spectator API (both v4 and v5) has been deprecated and removed by Riot Games.
 *
 * The application now uses match history polling to detect completed games.
 * See: src/league/api/match-history.ts
 */

// Keeping this file for now to avoid breaking imports, but all functions are deprecated
// and will throw errors if called
