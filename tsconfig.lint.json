{
    "extends": "./tsconfig.base.json",
    "include": [
        "scripts/**/*",
        "eslint.config.ts",
        "eslint-rules/**/*"
    ],
    "compilerOptions": {
        // Override noEmit to allow this config to work without composite projects
        "noEmit": true
    }
}
